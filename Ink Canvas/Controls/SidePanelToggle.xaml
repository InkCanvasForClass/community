<UserControl x:Class="Ink_Canvas.Controls.SidePanelToggle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="150" d:DesignWidth="80">
    <Grid Background="#02000000" Cursor="Hand" Width="80" Height="150" VerticalAlignment="Center">
        <!-- PanelBorder is the outer translucent track (width 24, height 110) positioned by C# code-behind -->
        <Border x:Name="PanelBorder"
                Width="24" Height="110"
                Background="Transparent"
                CornerRadius="12"
                VerticalAlignment="Center">
            <!-- Solid 100% Pure Black 1px Border Outline, positioned dynamically by C# -->
            <Border x:Name="InnerStripe"
                    BorderBrush="Black"
                    BorderThickness="1"
                    CornerRadius="4.5"
                    Width="9" Height="92"
                    VerticalAlignment="Center">
                <!-- Inner translucent stripe (width 7, height 90) that animates opacity -->
                <Border Background="Black"
                        CornerRadius="3.5"
                        Width="7" Height="90"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Opacity" Value="0.4"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                    <DataTrigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0.85" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.EnterActions>
                                    <DataTrigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0.4" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.ExitActions>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    
                    <Image x:Name="ChevronImage"
                           Visibility="Collapsed"
                           Source="{DynamicResource QuickPanelUnfoldChevronIcon}"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           Height="8" Width="8"
                           HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Image.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="0" Opacity="0.5"/>
                        </Image.Effect>
                    </Image>
                </Border>
            </Border>
        </Border>
    </Grid>
</UserControl>
