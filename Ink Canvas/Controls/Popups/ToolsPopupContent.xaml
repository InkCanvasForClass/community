<UserControl x:Class="Ink_Canvas.Controls.ToolsPopupContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             xmlns:controls="clr-namespace:Ink_Canvas.Controls;assembly=InkCanvas.Controls"
             xmlns:helpers="clr-namespace:Ink_Canvas.Helpers"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="AutoFitToolPopupLabel8" TargetType="Label">
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{DynamicResource FloatBarForeground}" />
            <Setter Property="helpers:AutoFontSizeHelper.IsEnabled" Value="True" />
            <Setter Property="helpers:AutoFontSizeHelper.MinFontSize" Value="3.5" />
            <Setter Property="helpers:AutoFontSizeHelper.MaxFontSize" Value="20" />
            <Setter Property="helpers:AutoFontSizeHelper.Step" Value="0.25" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <controls:PopupShellContent x:Name="Shell" Title="更多功能" d:Visibility="Collapsed" />
        <ContentControl x:Name="InnerContentHost" Visibility="Collapsed" d:Visibility="Visible">
            <ikw:SimpleStackPanel Margin="2" Spacing="3">
                <ikw:SimpleStackPanel.Resources>
                    <Style TargetType="Label" BasedOn="{StaticResource AutoFitToolPopupLabel8}" />
                </ikw:SimpleStackPanel.Resources>
                <ikw:SimpleStackPanel Orientation="Horizontal" Spacing="3">
                    <controls:ToolMenuButton x:Name="TimerToolBtn" Label="计时器" />
                    <controls:ToolMenuButton x:Name="RandomDrawToolBtn" Label="随机抽" />
                    <controls:ToolMenuButton x:Name="SingleDrawToolBtn" Label="单次抽" />
                </ikw:SimpleStackPanel>
                <ikw:SimpleStackPanel Orientation="Horizontal" Spacing="3">
                    <controls:ToolMenuButton x:Name="SaveToolBtn" Label="保存" />
                    <controls:ToolMenuButton x:Name="OpenToolBtn" Label="打开..." />
                    <controls:ToolMenuButton x:Name="ReplayToolBtn" Label="重播" />
                </ikw:SimpleStackPanel>
                <ikw:SimpleStackPanel Orientation="Horizontal" Spacing="3">
                    <controls:ToolMenuButton x:Name="ScreenshotToolBtn" Label="截屏" />
                    <controls:ToolMenuButton x:Name="ManualToolBtn" Label="说明书" />
                    <controls:ToolMenuButton x:Name="SettingsToolBtn" Label="设置" />
                </ikw:SimpleStackPanel>
            </ikw:SimpleStackPanel>
        </ContentControl>
    </Grid>
</UserControl>
