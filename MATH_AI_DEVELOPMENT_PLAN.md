# ICC CE 数学智能白板开发计划

## 1. 文档目标

本计划用于在 InkCanvasForClass Community Edition（ICC CE）现有白板能力之上，逐步增加平面几何、函数图像、立体几何、本地识别和可选云端 AI 功能。

开发必须遵循以下总原则：

- 不改变原有代码的功能、交互和模块职责，不用新功能替换现有白板、几何绘制、笔迹识别、PPT、页面或插件功能。
- 尽可能调用现有模块，仅在现有模块确实不能满足需求时增加最小、明确、可测试的扩展接口。
- 不借开发新功能重构、格式化、重命名或清理无关代码。
- 保持完整的功能链路：界面、交互、服务、数据模型、设置、持久化、本地化、异常处理和测试必须同步完成。
- 所有用户可配置功能必须接入设置窗口，遵循现有设置模型、设置页面、资源字符串、加载与保存流程。
- 开发前阅读与改动范围相关的 ICC CE 官方开发文档，并遵循仓库根目录 `AGENTS.md`、`CONTRIBUTING.md`、项目文件和 CI 工作流。
- 现有代码和 CI 是当前行为的事实依据；文档与代码不一致时，记录差异，不静默改变原有行为。
- 每个阶段应拆分为小型、可审查、可独立验证的 PR。

## 2. 产品定位

目标产品是一个离线基础能力完整、可选接入云端 AI 的数学智能白板：

```text
现有笔迹层
  自由书写、原有几何绘制、橡皮擦、PPT 批注
          ↓ 保持原功能不变
新增数学对象层
  点、线、圆、函数、立体、标签、约束和测量
          ↓
本地识别与计算层
  IACore、WinRT、表达式解析、几何计算和投影
          ↓
可选 AI 层
  自然语言、视觉识别、教学讲解和课堂辅助
```

AI 负责理解用户意图和生成结构化指令；最终数学对象、坐标、计算结果和渲染由本地确定性引擎产生。不得让大模型返回并执行任意 C#、PowerShell、脚本或系统命令。

## 3. 当前已有能力

以下模块已经存在，后续应优先复用：

| 已有能力 | 主要位置 | 新功能中的用途 |
|---|---|---|
| 形状绘制 | `MW_ShapeDrawing.cs`、`ShapeDrawPopupContent` | 保留为快速示意图；复用工具入口和绘制交互 |
| 墨迹转图形 | `MW_SimulatePressure&InkToShape.cs` | 为笔迹转数学对象提供候选识别结果 |
| 本地文字和图形识别 | `InkRecognitionManager`、`IpcIACoreClient`、`InkCanvas.IACoreHelper`、WinRT 识别器 | 作为本地 OCR 和图形识别基础 |
| 撤销和重做 | `TimeMachine`、`MW_TimeMachine.cs` | 扩展数学对象增删改历史 |
| 页面管理 | `MW_PageListView.cs`、`MW_BoardControls.cs` | 每页维护独立数学场景 |
| 保存和恢复 | `MW_Save&OpenStrokes.cs` | 保存数学场景及版本信息 |
| 工具栏 | 浮动工具栏、白板工具栏及其 Registry | 添加数学工具入口 |
| 插件系统 | `InkCanvas.PluginSdk`、`PluginManager` | 在不扩大主窗口职责的前提下扩展能力 |
| 设置系统 | `Resources/Settings.cs`、设置页面和 `SettingsManager` | 配置数学、识别和 AI 功能 |
| 本地化和主题 | `.resx`、`Strings`、`LocalizationHelper`、动态资源 | 保持语言、浅色/深色主题和 DPI 一致 |
| 安全和隐私 | `SecurityManager`、隐私设置、插件安全和日志 | 保护 API Key、上传内容和模型请求 |

现有圆、抛物线、双曲线、长方体、圆柱和圆锥等工具生成的是笔迹式图形。它们应继续保留，不应被新数学对象系统替换。

## 4. 新旧功能边界

### 4.1 现有笔迹图形

- 保持现有工具栏、绘制方式、保存方式和撤销行为。
- 继续适用于快速课堂示意、自由标注和 PPT 批注。
- 不强制转换为结构化数学对象。

### 4.2 新增数学对象

- 拥有唯一 ID、对象类型、结构数据、样式、来源和版本。
- 可以选择、移动、编辑、测量、删除、撤销、保存和恢复。
- 可以由用户手动创建，也可以由本地识别或 AI 生成。
- 与普通笔迹共存，不接管现有 InkCanvas 输入路径。

### 4.3 识别和 AI 结果

- 必须先显示候选或预览，由用户确认后插入。
- 默认保留原始笔迹或图片。
- 一次批量生成作为一个原子操作撤销。
- 识别或模型失败不得修改当前页面。

## 5. 第一版交付范围

第一版目标：

> 在现有白板上创建、编辑、保存、恢复和撤销点、线段、圆、标签与函数图像，并支持本地手写识别后由用户确认插入数学对象。

第一版包含：

- 点、线段、直线、圆和文本标签。
- 网格、坐标轴和基础吸附。
- 距离、角度和半径测量。
- `y=f(x)` 函数图像。
- 函数零点和交点标记。
- 本地识别简单文字、变量和函数表达式。
- 选择笔迹后转换为文本、函数或基础几何对象。
- 撤销、重做、页面切换、保存和恢复。
- 设置窗口中的数学功能、识别行为和显示设置。

第一版不包含：

- 完整动态几何约束求解器。
- 通用数学公式 OCR。
- 自动证明和复杂符号推导。
- 云端大模型和视觉模型。
- 完整三维建模、复杂截面和网格编辑。
- 自动解题、课堂分析和讲义生成。

## 6. 初始代码组织

第一阶段只在主项目中增加一个职责清晰的 `Math` 模块，不预先创建多个用途不明确的程序集：

```text
Ink Canvas/
  Math/
    Models/
      MathScene.cs
      MathObject.cs
      PointObject.cs
      SegmentObject.cs
      CircleObject.cs
      FunctionGraphObject.cs
      TextLabelObject.cs
    Rendering/
      MathCanvasControl.cs
      MathSceneRenderer.cs
    Services/
      MathSceneService.cs
      MathExpressionParser.cs
      MathRecognitionService.cs
    Persistence/
      MathSceneSerializer.cs
```

纯计算代码稳定并出现明确复用需求后，再评估抽取独立类库。不得仅为了“架构完整”提前增加空项目或单用途抽象层。

## 7. 数学对象与画布方案

### 7.1 每页数学场景

每个白板页维护一个 `MathScene`：

```text
MathScene
  ├─ SchemaVersion
  ├─ Objects
  ├─ Viewport
  ├─ GridSettings
  ├─ Selection
  └─ SourceStrokeReferences
```

每个对象至少包含：

- 唯一 ID。
- 对象类型。
- 结构数据。
- 可见和锁定状态。
- 颜色、线宽、线型等样式。
- Z-Order。
- 来源：手动、识别、AI 或导入。
- 原始笔迹引用。

### 7.2 画布接入

数学渲染层作为现有 InkCanvas 的子层存在：

```text
InkCanvas
  ├─ 原有笔迹
  ├─ 原有图片和媒体元素
  └─ MathCanvasControl
       ├─ 几何对象
       ├─ 函数曲线
       ├─ 文本标签
       └─ 立体几何投影
```

数学层负责自身命中测试、选择、移动和编辑，不改变原有笔迹、橡皮擦、手势和 PPT 批注流程。

### 7.3 历史记录

在现有 TimeMachine 基础上增加数学对象操作：

- `AddMathObject`
- `RemoveMathObject`
- `UpdateMathObject`
- `BatchMathOperation`

历史项应保存可恢复的数据快照或可逆命令，不只保存 UI 控件引用。

### 7.4 保存格式

- 现有笔迹文件保持兼容。
- 数学场景使用带 `SchemaVersion` 的 JSON。
- 如果 `.icstk` 为容器格式，则增加数学场景条目；否则采用与原文件关联的兼容元数据方案。
- 旧文件没有数学场景时必须正常打开。
- 未知对象或损坏对象应被隔离，不得导致整页加载失败。
- 保存采用现有原子写入、备份和恢复路径。

## 8. 分阶段开发

## M0：数学层接入

目标：验证结构化数学对象可以安全进入现有页面、历史和保存链路。

开发内容：

- 添加 `MathScene` 和基础对象模型。
- 添加 `MathCanvasControl`。
- 添加点、线段、圆和标签的渲染。
- 每页维护独立数学场景。
- 接入新增、删除和移动操作。
- 接入撤销与重做。
- 接入页面切换、保存和恢复。
- 在现有工具栏增加“数学”入口。
- 在设置窗口增加数学功能总开关和基础显示设置。

验收标准：

- 可以创建、选择、移动和删除点、线段、圆和标签。
- 页面切换后对象完整恢复。
- 文件保存后重新打开仍可编辑。
- 所有操作可以撤销和重做。
- 关闭数学功能后原有程序行为与开发前一致。
- 普通笔迹、现有形状工具、橡皮擦、PPT 和触控操作不受影响。

## M1：平面几何 MVP

目标：形成可以动态编辑和测量的基础平面几何。

开发内容：

- 点、线段、直线、射线和圆。
- 网格、坐标轴和刻度。
- 端点、中点、圆心和交点吸附。
- 两线交点、线圆交点和圆圆交点。
- 距离、角度、半径和坐标标注。
- 拖动点时关联对象实时更新。
- 首批约束：共线、水平、垂直、等长、点在线上、点在圆上。
- 选中现有笔迹后，调用现有识别模块生成几何对象候选。
- 在设置窗口增加网格、吸附、标注和约束相关设置。

验收标准：

- 拖动点后关联对象和测量值正确更新。
- 交点能够作为后续对象的引用点。
- 约束冲突有明确提示，不破坏现有场景。
- 现有笔迹几何工具行为保持不变。
- 300 个基础对象的测试场景仍可正常交互。

## M2：函数图像 MVP

目标：实现表达式驱动的函数绘图。

开发内容：

- 安全的本地表达式解析器。
- 支持 `+ - * / ^ ()`。
- 支持 `sin`、`cos`、`tan`、`sqrt`、`abs`、`log` 和 `exp`。
- 支持 `y=f(x)`、定义域和多函数叠加。
- 支持坐标轴、网格、刻度、平移和缩放。
- 实现自适应采样和不连续点检测。
- 支持零点、交点和基础极值标记。
- 支持函数颜色、线宽和可见性设置。
- 在设置窗口增加采样质量、坐标范围、网格和标注设置。

实现要求：

- 不使用 `eval`、动态代码生成或任意脚本执行。
- 对表达式长度、递归深度、采样数量和计算时间设置上限。
- 数值和符号结论由本地计算或后续 CAS 获取并验证，不使用大模型猜测。
- 现有抛物线、双曲线和坐标系笔迹工具继续保留。

验收标准：

- 正确绘制 `y=x^2-4`、`y=sin(x)` 和 `y=1/x`。
- 不连续函数不会跨越无定义区间错误连线。
- 非法表达式显示明确错误且不修改场景。
- 保存后表达式仍可编辑。

## M3：本地 OCR 与笔迹转数学对象

目标：复用 IACore 和 WinRT，实现离线的“手写 → 数学对象”流程。

用户流程：

```text
圈选笔迹
  → 调用本地识别
  → 显示候选结果
  → 用户确认或修改
  → 插入文本、函数或几何对象
```

开发内容：

- 增加圈选笔迹识别入口。
- 复用 `InkRecognitionManager`、`IpcIACoreClient` 和 WinRT 回退。
- 显示候选文字、包围框和识别引擎信息。
- 将普通文字转换为可编辑标签。
- 将简单 `y=...` 结果交给本地表达式解析器。
- 将圆、线、矩形等结果转换为几何候选。
- 记录来源笔迹引用。
- 在设置窗口增加识别引擎、确认方式、原笔迹保留策略和超时设置。

实现要求：

- 不复用自动手写体美化的“直接替换”交互作为数学识别流程。
- 数学识别必须先预览并确认。
- 原始笔迹默认保留。
- 识别异常、超时或 Helper 崩溃不得改变页面。

验收标准：

- 手写 `y=x²+1` 可经用户确认生成函数对象。
- 手写圆或直线可经确认生成几何对象。
- 可以修改候选文本后再插入。
- 可以撤销转换并恢复原始状态。
- 无网络时完整可用。

## M4：2.5D 立体几何

目标：完成适合课堂演示的结构化空间几何，不立即建设完整建模系统。

首批对象：

- 正方体、长方体、棱柱和棱锥。
- 圆柱、圆锥和球。

开发内容：

- 三维顶点、棱、面和标签模型。
- 二维投影。
- 实线、虚线和隐藏棱。
- 标准视角、旋转、平移和缩放。
- 棱长、角度、表面积和体积标注。
- 保存相机和对象状态。
- 在设置窗口增加投影模式、隐藏线、标签和渲染质量设置。

实现要求：

- 复用现有长方体、圆柱和圆锥工具的视觉与交互经验。
- 不改变现有笔迹式立体图形工具。
- 第一阶段不引入复杂三维依赖；现有 WPF 能满足时优先使用现有平台能力。

验收标准：

- 可以创建、旋转、保存和恢复基本立体。
- 旋转不会改变模型尺寸和测量结果。
- 隐藏棱和标签随视图正确更新。
- 低性能设备可以降低渲染质量。

## M5：云端 AI 与视觉识别

前置条件：M1、M2、M3 已稳定并具有可靠的本地数学对象协议。

开发内容：

- 定义文本与视觉模型 Provider 接口。
- 增加模型配置、能力探测、取消、超时、限流和错误处理。
- 使用系统安全存储保存 API Key。
- 增加用户上传确认和隐私控制。
- 定义带版本的数学命令 JSON Schema。
- 验证模型输出中的类型、ID、数值范围、表达式、引用和对象数量。
- 显示生成预览，确认后批量插入。
- 在设置窗口增加 AI 总开关、模型服务、模型名称、连接测试、上传权限和历史保留设置。

第一批能力：

- “画出 `y=x^2-4`”。
- “生成一个圆心为 A、半径为 3 的圆”。
- “作 AB 的中垂线”。
- “生成一个正方体并标出对角线”。
- 从选区图片中提取函数或几何条件。

模型只允许返回结构化命令：

```json
{
  "schemaVersion": 1,
  "commands": [
    {
      "type": "CreateFunction",
      "expression": "x^2-4",
      "domain": [-5, 5]
    }
  ]
}
```

本地验证通过后才能执行。

验收标准：

- 非法、缺失或超范围的模型结果被安全拒绝。
- AI 无法执行本地代码或访问未授权文件。
- 一次 AI 生成可以整体撤销。
- 云端不可用时本地数学和识别功能正常。
- 未授权时不上传图片、笔迹或课堂内容。

## M6：课堂智能功能

前置条件：数学对象层和 AI 协议稳定。

开发内容：

- 自动建议辅助线。
- 根据函数图像生成性质说明。
- 从几何场景提取已知条件。
- 根据当前场景生成练习题和参数变式。
- 分步提示模式。
- 检查绘图是否满足指定条件。
- 将多页白板整理为课堂讲义。
- 导出 SVG、PNG、结构化数学场景和 PPT 素材。
- 在设置窗口增加教学模式、提示级别、自动建议和导出设置。

AI 输出必须标明结果来源：

- 本地确定性计算。
- CAS 计算与验证。
- 几何数值检查。
- 模型推测。
- 尚未验证。

## 9. 设置窗口开发要求

任何新增用户可配置功能必须同时完成以下链路：

1. 在现有 Settings 模型中增加字段和默认值。
2. 在对应设置页面增加符合现有控件规范的 UI。
3. 使用 `.resx` 提供用户可见字符串，不硬编码中英文。
4. 在页面加载时读取设置。
5. 在设置变化时保存并通知运行模块。
6. 在应用启动和页面恢复时应用设置。
7. 为旧配置缺失字段提供兼容默认值。
8. 必要时增加配置迁移和验证。
9. 在浅色、深色、高 DPI 和触控环境检查界面。

设置分类建议：

- 数学画布：总开关、网格、坐标轴、吸附和标注。
- 函数图像：采样质量、坐标范围、线型和显示项目。
- 立体几何：投影、隐藏线、标签和渲染质量。
- 本地识别：引擎、候选确认、原笔迹策略和超时。
- AI：总开关、Provider、模型、连接测试和超时。
- 隐私：图片/笔迹上传权限、历史保留和缓存清理。

## 10. 前后端完整性要求

ICC CE 是 WPF 桌面应用，本计划中的“前后端完整”表示每项功能必须覆盖以下层次：

| 层次 | 必须完成 |
|---|---|
| 界面层 | 工具入口、编辑控件、候选/错误/加载状态、主题和本地化 |
| 交互层 | 鼠标、触控、笔、选择、拖动、取消和撤销 |
| 服务层 | 计算、识别、AI 调用、超时、取消、错误回退 |
| 数据层 | 对象模型、输入验证、版本和引用完整性 |
| 设置层 | 设置窗口、默认值、保存、加载和运行时应用 |
| 持久化层 | 页面切换、文件保存、恢复、迁移和损坏隔离 |
| 安全层 | 权限、密钥、路径、网络、隐私和日志脱敏 |
| 测试层 | 单元测试、集成测试、UI 检查和性能检查 |

不得只完成界面按钮而没有底层实现，也不得只完成服务或模型而没有设置、交互、错误提示和持久化。

## 11. 测试计划

### 11.1 单元测试

- 数学对象验证。
- 序列化、反序列化和版本迁移。
- 表达式解析和错误定位。
- 函数采样与不连续点检测。
- 几何求交、测量和基础约束。
- 三维投影和旋转不变量。
- OCR 结果到数学对象的转换。
- AI JSON Schema 和命令校验。
- 设置默认值和迁移。

### 11.2 集成测试

- 数学对象与 TimeMachine。
- 数学对象与页面切换。
- 数学对象保存、关闭和恢复。
- IACore 超时、崩溃和 WinRT 回退。
- AI 请求取消、断网和错误输出。
- 插件加载和卸载。
- 普通笔迹、图片、PPT 和数学对象共存。

### 11.3 UI 和运行验证

- 鼠标、触控和手写笔。
- 浅色和深色主题。
- 多显示器和高 DPI。
- 批注模式、白板模式和 PPT 模式。
- 设置即时生效和重启后保持。
- 识别/AI 的加载、取消、失败和重试。

### 11.4 性能测试

- 300 个基础二维对象。
- 20 条函数曲线。
- 大量采样点。
- 多页数学场景。
- 连续拖动、缩放和旋转。
- 多次 OCR/AI 请求后的内存占用。
- 数学功能关闭时与原版本的性能对比。

## 12. 回归保护

每个阶段至少检查以下现有功能：

- 普通笔迹输入和压感。
- 墨迹平滑和湿墨渲染。
- 橡皮擦和选择操作。
- 现有形状绘制。
- 墨迹转图形。
- 手写体美化。
- 撤销和重做。
- 页面新增、删除和切换。
- 文件保存和打开。
- 图片和媒体元素。
- PPT 连接、翻页和笔迹保存。
- 浮动工具栏和白板工具栏。
- 插件加载和设置页。
- 主题、本地化和 DPI。

数学功能关闭时，以上行为应与开发前一致。

## 13. PR 顺序

建议按以下顺序提交：

1. `feat(math): add per-page math scene persistence`
2. `feat(math): add math canvas and basic object rendering`
3. `feat(math): integrate math object history with TimeMachine`
4. `feat(math): add geometry selection and dragging`
5. `feat(math): add snapping, intersections and measurements`
6. `feat(math): add safe function expression parser`
7. `feat(math): add cartesian function graph rendering`
8. `feat(recognition): add selected ink recognition preview`
9. `feat(recognition): convert confirmed results to math objects`
10. `feat(math): add 2.5D solid geometry`
11. `feat(ai): add validated math command contract`
12. `feat(ai): add optional text and vision providers`
13. `feat(ai): add classroom assistance features`

每个 PR 必须说明：

- 目标和非目标。
- 修改范围。
- 复用的现有模块。
- 设置和本地化变化。
- 数据格式或迁移影响。
- 测试命令、环境和结果。
- 未验证边界。
- UI 截图或交互录屏（如适用）。
- 回滚方式。

## 14. 官方开发文档要求

开发前必须根据改动范围查阅 ICC CE 官方中文开发文档。`/website/cn/dev/` 根地址可能返回 404，应访问带具体后缀的页面。

重点文档：

- 系统架构、应用入口、事件驱动、全局服务和配置管理。
- 核心模块、白板系统、页面管理、形状绘制和手势交互。
- 开发环境、代码规范、调试测试和贡献流程。
- 自定义控件、数据绑定、模板样式和性能。
- 插件架构、插件生命周期、宿主服务、配置和打包。
- 工具栏系统、注册中心和工具栏项实现。
- UI 系统、主题、多语言和弹窗系统。
- InkCanvas 核心、笔迹平滑、渐隐和颜色笔刷管理。
- 配置 API、IPC API、插件 API、工具栏 API 和 PPT API。
- 高级模块中的文件、异常恢复、遥测和安全。
- 部署、构建发布、监控维护和故障排除。

当新增工具、服务或 API 时，必须同步更新：

- 实现代码。
- 接口或协议定义。
- 设置模型和设置页面。
- 本地化资源。
- 示例或使用说明。
- 测试和回归验证。

## 15. 完成定义

一项功能只有同时满足以下条件才能标记完成：

- 功能在目标模式下真实可用。
- 原有功能无已知回归。
- 设置窗口可以配置相关行为。
- 设置能够保存、加载并在运行时正确应用。
- 用户可见内容完成本地化。
- 数据可以随页面和文件保存恢复。
- 错误、取消和不可用状态有明确处理。
- 相应测试已经运行并通过。
- 文档和 PR 描述完整。
- 未验证项目被明确记录，没有把未执行的验证写成已完成。
