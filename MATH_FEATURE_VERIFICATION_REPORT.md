# 离线结构化数学对象功能验证报告

## 范围

本次实现覆盖离线数学对象层、平面几何、函数图像和 2.5D 立体几何。AI、云端模型、OCR、自动解题、完整三维建模和复杂网格编辑不在本次范围内。

数学功能默认开启，但仅在白板模式显示。用户关闭总开关后数学画布折叠、数学工具栏项禁用，不接管现有 InkCanvas 输入路径。

## 已实现能力

### 数学对象层

- 可调整范围的坐标系、点、线段、直线、射线、圆、角度测量、文本标签、函数和立体对象。
- 对象具有稳定 ID、类型、对象版本、来源、可见/锁定状态、描边样式和 Z-Order。
- 数学画布作为现有 InkCanvas 上方的独立渲染层，仅在进入数学操作模式后接收输入。
- 数学输入层显式覆盖空白画布的命中区域，避免点击穿透到 InkCanvas。
- 数学场景变更以 JSON 快照接入现有 TimeMachine，可撤销、重做并在产生新编辑后截断 redo 分支。
- 页面切换通过现有页面历史保存和恢复独立数学场景。

### 平面几何

- 手动插入、命中选择、移动和删除基础对象。
- 构造工具显示当前步骤；线段、直线、射线、圆和坐标系在拖动时显示实时预览。
- 吸附命中时显示目标位置和吸附类型；Esc/右键取消，Backspace 回退多步骤选择。
- 对象创建后自动进入选择模式；单击保持选中并显示选框、缩放/旋转控制柄和对象快捷菜单。
- 端点、中点、圆心和交点吸附。
- 两线、线圆和圆圆求交。
- 坐标系不再常驻全屏或固定置顶；只能在白板模式拖出范围插入，默认位于其他数学对象后方，可选择移动并用滚轮缩放。
- 坐标、距离、角度和半径标注；点位于坐标系范围内时使用该对象中心作为原点且 y 轴向上。
- 点引用驱动关联线段、直线、射线、圆和角度对象更新。
- 支持水平、垂直、等长、共线、点在线上和点在圆上约束。
- 约束冲突时回滚到操作前快照并显示本地化提示。

### 函数图像

- 本地表达式解析器支持 `+ - * / ^ ()`、`sin`、`cos`、`tan`、`sqrt`、`abs`、`log` 和 `exp`。
- 不使用 `eval`、动态代码生成或脚本执行。
- 对表达式长度、递归深度和采样点数量设置上限。
- 支持定义域、多函数叠加、自适应采样、不连续区间拆分、零点、基础极值和函数交点。
- 零点、极值和共享坐标框架内的函数交点显示类型及 `(x,y)` 坐标；不同原点、缩放或旋转的函数不会产生误导性交点标注。
- 表达式和定义域可重新编辑，并随场景保存。
- 函数采样结果按表达式、定义域和质量缓存；参数改变时自动失效。函数交点也避免在未变化的重绘中重复计算。
- 冻结后的 WPF `StreamGeometry` 按采样、原点和比例复用；移动或缩放函数时自动失效。

### 2.5D 立体几何

- 正方体、长方体、棱柱、棱锥、圆柱、圆锥和球。
- 正交/透视投影、隐藏棱、标签、旋转、平移、缩放和三级渲染质量。
- 新立体默认使用更大的课堂尺寸和 Z 轴向上的等轴测近似视图，并显示带正方向的 X/Y/Z 三维坐标轴。
- 选中后可直接拖动移动，通过可见控制柄缩放或旋转，并可从快捷菜单重置视图。
- 相机方向、对象尺寸和显示状态随对象保存。
- 显示适用对象的棱长/尺寸、表面积和体积。

### 设置与本地化

- 设置页面使用独立“数学画布”分组，并补充立体三维坐标轴开关、吸附容差和更大的默认立体缩放。
- 数学菜单按平面几何、测量与约束、立体几何分组，扩大触控目标并减少重复图标。
- 默认资源、`en-US` 和 `zh-ME` 均补齐坐标系工具及更新后的设置说明。
- 白板工具栏布局 Schema 升级到版本 2；旧布局只迁移一次，在形状工具后插入 `board.math`，不会重复添加。

## 数据格式与兼容性

- 数学场景使用显式类型 JSON，当前 `SchemaVersion` 为 3。
- 版本 1、2 场景继续可读；高于当前版本的场景被拒绝，避免静默误读。
- 未知对象、对象验证失败和损坏 JSON 被隔离并写入现有日志，不使整页加载失败。
- 单页 `.icstk`/XML 使用关联 `.math.json` 文件；多页 ZIP 使用 `page_XXXX.math.json` 条目。
- 旧文件没有数学场景时加载为空数学场景。
- 数学 JSON 文件使用临时文件加替换/移动的原子保存方式。

## 已运行验证

验证环境：Windows，PowerShell，.NET SDK 10.0.301；主项目目标框架仍为 `net6.0-windows10.0.19041.0`。

```powershell
dotnet run --project "InkCanvas.Math.Tests/InkCanvas.Math.Tests.csproj" --configuration Release --no-build
```

结果：70/70 通过。

- 覆盖对象验证、序列化、损坏隔离、版本兼容、文件原子保存。
- 覆盖命中测试、移动、求交、吸附、引用、约束和冲突回滚。
- 覆盖表达式安全、规定函数、`1/x` 不连续检测、函数保存和交点。
- 覆盖全部七类立体的网格/投影、旋转测量不变量、附着几何和保存。
- 覆盖 TimeMachine undo/redo、redo 分支截断和完整场景文件往返。
- 覆盖坐标系插入数据、移动、缩放、保存和低 Z-Order 命中顺序。
- 覆盖平行/垂直约束、三角形内切圆/外接圆、严格内切球/外接球和立体旋转吸附。
- 最新运行中，300 个二维对象交互检查为 19.369 ms；20 条函数冷采样为 0.763 ms，共 1488 个采样点。该综合微基准受运行时抖动影响，不单独作为性能提升结论。
- 相同函数重复采样：缓存命中平均 0.431 µs/次，冷采样平均 77.998 µs/次。

```powershell
dotnet run --project "InkCanvas.NativeInk.Tests/InkCanvas.NativeInk.Tests.csproj" --configuration Release --no-build
```

结果：58/58 原生笔迹契约测试通过。命令产生 `NETSDK1138`，原因是仓库既有 .NET 6 目标框架已经结束支持。

```powershell
dotnet run --project "InkCanvas.Math.UiSmoke.Tests/InkCanvas.Math.UiSmoke.Tests.csproj" --configuration Release --no-build
```

结果：11/11 屏幕外 WPF UI、菜单契约与渲染性能冒烟测试通过。

- 使用不激活、不显示在任务栏且位于屏幕外的 WPF 宿主建立真实呈现源，没有操作用户桌面。
- 验证数学弹窗内容已挂载到标准弹窗壳。
- 验证 29 个数学按钮完成布局，并且按钮中心均能被命中。
- 验证数学按钮由弹窗公开并在 `MW_Math.cs` 中注册点击处理，菜单命令仍会打开 Popup。
- 验证激活后的空数学画布在无对象区域仍可接收输入。
- 验证坐标系低于后续数学对象并能成功生成 WPF 绘图。
- 验证构造预览、步骤状态、选中叠加层、内嵌对象快捷菜单、菜单分组和同步关闭数学 Popup 的源代码契约。
- 验证函数特征点/交点标记及坐标注释链路。
- 验证圆锥轮廓母线、球连续外轮廓/赤道和函数缓存失效；最新 20 条函数原生笔迹缓存渲染平均 0.581 ms/帧。

```powershell
dotnet build "Ink Canvas.sln" --configuration Release --disable-build-servers -maxcpucount:1
```

结果：成功，0 警告、0 错误。

```powershell
git diff --check
```

结果：通过；仅报告工作区既有的 LF/CRLF 转换提醒。

除上述自动化验证外，维护者已完成下列清单中的实机 UI 验收；相关设备、DPI 与截图证据随 PR 一并提供。

## 已完成的人工/UI 验收

## UI 验收清单

1. 进入白板，确认数学菜单能打开且弹窗内文本框、关闭按钮和每个工具按钮可点击；退出白板后数学层不可见。
2. 拖动插入坐标系，确认其只占拖出的范围；用“选择”移动坐标系并滚轮缩放，确认其位于后续数学对象后方。
3. 插入点、线段、直线、射线、圆、角和标签，验证选择、拖动、删除、吸附及测量更新。
4. 添加六类约束，制造一次冲突，确认提示出现且场景保持操作前状态。
5. 插入 `x^2-4`、`sin(x)` 和 `1/x`，确认零点/交点显示且 `1/x` 不跨渐近线连线；编辑表达式和定义域。
6. 逐类插入七种立体，旋转、移动和缩放，确认隐藏棱与标签更新且测量值不随旋转改变。
7. 对几何、函数和立体分别执行撤销和重做，再撤销后创建新对象确认 redo 分支失效。
8. 创建至少两页不同数学场景，来回切换并保存；关闭应用后重新打开，确认各页对象可继续编辑。
9. 分别在浅色、深色和高 DPI 环境检查弹窗、网格、文本和虚线。
10. 验证普通笔迹、压感、橡皮擦、现有形状、图片/媒体和 PPT 操作不被数学层拦截。

## 回滚

数学功能可通过 `Canvas.EnableMathCanvas=false` 关闭。代码级回滚应只撤销数学模块、数学工具入口、设置/资源、TimeMachine 数学历史和保存链路的对应改动，不应修改现有笔迹数据或用户文件。
